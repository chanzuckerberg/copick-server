stack:
  services:
    copick-server:
      image:
        tag: sha-d886cd9
      replicaCount: 1
      env:
        - name: PORT
          value: "8012"
        - name: HOST
          value: "0.0.0.0"
        - name: OVERLAY_ROOT
          value: /mnt/copick/overlay
